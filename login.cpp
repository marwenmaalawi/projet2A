#include "login.h"
#include <QDebug>

login::login()
{

}
/*query.prepare("SELECT id,mdp from admin where id=:ID");
query.bindValue(":ID",ID);
query.exec();
  while(query.next()){
    id=query.value(0).toString();
  mdp=query.value(1).toString();
  }
  if((id==ID)&&(mdp==MDP)){
      return  true;
  }else{
      return  false;
  }
*/
